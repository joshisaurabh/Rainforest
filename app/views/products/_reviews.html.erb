<% @product.reviews.each do |review| %>
<p>
      <%= review.comment %>
      <br>
      <% if review.user != nil && review.comment != nil %>
        by <em> <%= review.user.name %></em>
        Added on: <%= review.created_at %>
      <% end %>
</p>
<% end %>